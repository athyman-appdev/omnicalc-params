<h1>Word Count Results</h1>

<dl>
    <dt>Text</dt>
    <dd class="font-weight-normal"><%= @word_count_input %></dd>
    
    <dt>Word Count</dt>
    <dd class="font-weight-normal"><%= @word_count_input.split(" ").size %></dd>
    
    <dt>Character Count (with spaces)</dt>
    <dd class="font-weight-normal"><%= @word_count_input.length %></dd>
    
    <dt>Character Count (without spaces)</dt>
    <dd class="font-weight-normal"><%= @word_count_input.gsub(/\s+/, "").length %></dd>
    
    <dt>Occurrences of <%=@special_word%></dt>
    <dd class="font-weight-normal"><%= @special_word_count %></dd>
    
    <a href="/word_count/new">Process more text</a>
</dl>